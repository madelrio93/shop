/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            protocol: 'https',
            hostname: 'trkysofdaodgpnsxmruz.supabase.co'
        }]
    }
}

module.exports = nextConfig
